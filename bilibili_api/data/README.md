此处存放了模块需要的相关数据。

- `api`: 所有接口的数据，JSON 格式，兼容 `Api` 类。移植此处数据无需通知作者。(**GPL v3**)
- `geetest`: 极验验证页面，基于 <https://github.com/kuresaru/geetest-validator> 编写。(_no license_)
- `browser_fingerprint.json`: 模块默认使用的浏览器指纹数据，由 `fpgen` 生成。(_no license_)
- `**.json`: 部分接口数据缓存。(_no license_)
